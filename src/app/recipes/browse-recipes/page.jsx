import { getRecipes } from "@/lib/data";

const BrowseRecipesPage = async () => {
  // const recipes = await getRecipes();
  // console.log(recipes);
  return <h1 className="container text-3xl my-6">Browse Recipes</h1>;
};
export default BrowseRecipesPage;
